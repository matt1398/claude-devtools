# Dependencies
node_modules/

# Build output
dist/
dist-electron/
dist-standalone/
out/
release/
coverage/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local machine setup — never publish. Keep your host-specific hardening,
# update scripts and paths here so `git pull` stays conflict-free and no
# environment detail reaches the public history. See scripts/leak-check.mjs.
docker-compose.override.yml
update-devtools.sh
*.local.sh

# Claude Code session transcripts — these contain real conversation content
# and must never be committed, even as test fixtures.
*.jsonl

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# TypeScript
*.tsbuildinfo

.pnpm-store/
package-lock.json
notification_example/
temp/
.claude/*.local.json
.claude/agent-memory/*
.claude/plans/*
claude-dev.tools/*

eslint-fix/
remotion/*